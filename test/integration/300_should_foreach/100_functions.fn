function "GetItems" {
  description "Returns a list of items"
  path "{{ libPath }}/items"

  output "items"
  output "count"
}

function "ForeachFunc" {
  description "Function to be looped several times"
  path "{{ libPath }}/foreach"

  input "item"
  input "count"

  output "value"
}